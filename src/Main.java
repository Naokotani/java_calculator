/**
 * Main class. Entry point for app
 */
public class Main {
    /**
     * Main function. Creates an instances of CalcInput to generate the UI.
     * @param args
     */
    public static void main(String[] args) {
        new CalcInput();
    }
}